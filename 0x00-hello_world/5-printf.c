#include <stdio.h>

/**
 * main - A simple program to output a line of text
 *
 * Return: 0 on completion
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
