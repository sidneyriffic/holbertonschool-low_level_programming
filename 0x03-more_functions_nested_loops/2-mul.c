/**
 * mul - returns the multiple of two numbers
 *
 * @a: one number to multiply
 * @b: another number to multiply
 *
 * Return: multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
